[build]
buildCommand = "pip install -e '.[dev]' && python -m cli.main build --refresh --username southwestmogrown --token $GITHUB_TOKEN --embedder local"

[deploy]
startCommand = "python -m cli.main serve --username southwestmogrown --llm anthropic --port $PORT"
