[build]
buildCommand = "pip install --upgrade pip && pip install -e '.' && python -m cli.main build --refresh --username southwestmogrown --embedder openai"

[deploy]
startCommand = "python -m cli.main serve --username southwestmogrown --llm anthropic --port $PORT"